#!/bin/bash
set -e

if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
    if [ -x /usr/share/openmediavault/mkconf/certbot ]; then
        /usr/share/openmediavault/mkconf/certbot remove || true
    fi
fi

exit 0
